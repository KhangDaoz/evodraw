# Backend the desktop app connects to. Baked into the build at build time
# (see vite.main.config.mjs). Copy to .env to override locally, e.g. point at a
# local server during development:
#   DEFAULT_SERVER_URL=http://localhost:4000
DEFAULT_SERVER_URL=https://evodraw-v9rt.onrender.com
